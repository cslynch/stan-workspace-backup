## OpenClaw Update Available

**Version:** 2026.5.7 (current: 2026.2.6)  
**Released:** May 7, 2026  
**Status:** 3 minor versions ahead

### Relevant Changes

**Telegram & Channel Communication**
- Fixed polling watchdog to stay tied to `getUpdates` liveness (was masked by unrelated Bot API calls)
- Fixed sender allowlist enforcement for Telegram DMs, groups, and native commands
- Fixed model provider ID parsing in `/models` callback buttons (now handles dots in provider names)
- Improved message delivery tracking for same-chat sends

**Gateway & Session Management**
- Gateway now clears cached skills snapshots during `/new` and session resets (fixes stale skill lists)
- Fixed daily session rollover to persist new transcript files with updated session IDs
- Improved task reconciliation for stale CLI run-context tasks
- Subagent registry now honors configurable retention policy instead of hardcoded 5-minute TTL

**Authorization & Security**
- Native command handlers now properly honor owner enforcement
- Active Memory toggles now require admin scope
- Auto-reply gates inline skill tool dispatch through authorization hooks

**Infrastructure**
- Tavily search/extract credentials now resolved from active runtime config snapshot (fixes SecretRef handling)
- Agent delivery now correctly reports `deliverySucceeded=false` when no adapter result available
- Cron isolated runs now fail delivery *before* model execution when no route exists

### Recommendation

**REVIEW**

This release contains important infrastructure improvements for Telegram reliability, session management, and authorization handling. No security vulnerabilities flagged, but multiple fixes affecting core communication paths and credential handling warrant attention.

---
*Briefing generated by OpenClaw Release Monitor at 2026-05-09 05:50 CT*
